# ADR-004：DFM/成本先判适用性，再要求可复测基线

- 状态：已接受（2026-08-13）
- 决策：DFM 仅在涉及硬件、BOM、制造、装配、工艺、供应链或量产时 `required`；纯软件项目记录 `not_applicable` + 理由，不生成空泛 DFM 文档。适用时必须先建立可复测基线，再由用户批准量化阈值；无基线不得编造数字（阻断或由用户明确接受残余风险）。软件项目按适用性评估构建/部署/云/数据库/外部 API/性能成本。
- 后果：quality.md 的检查单把“无基线即阻断”列为硬规则。
