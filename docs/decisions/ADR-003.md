# ADR-003：仪表盘为旁路只读消费者

- 状态：已接受（2026-08-13）
- 决策：dashboard 是独立可选进程，仅 GET/HEAD，绑定 127.0.0.1:0，全内存路由（不做请求路径→文件系统解析），只读当前 revision 的 snapshot envelope；失败/被杀/超时不影响编排（编排无 await/无依赖/不因 dashboard 写事件）。
- 备选：dashboard 与编排同进程、或提供写操作/控制按钮 —— 未选，耦合与越权面过大。
- 后果：页面只能查看，不能批准/重试/恢复；那些动作必须走主会话脚本。
