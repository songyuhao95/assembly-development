# 任务级测试用例规范（模板）

任务：`<TASK_ID>` 合同：`<CONTRACT_ID>`

每条用例必须包含：输入、操作、预期行为、验证方法、证据路径、Pass/Fail。至少覆盖（按适用性）：

| 场景类别 | 必选 | 用例 |
|---|---|---|
| 正常路径 | ✔ | |
| 无效输入 | ✔ | |
| 边界值 | ✔ | |
| 权限不足 | ✔ | |
| 空/错/慢状态 | 按需 | |
| 重复调用/幂等 | 按需 | |
| 并发 | 按需 | |
| 超时/重试 | 按需 | |
| 外部依赖失败 | 按需 | |
| 恢复 | 按需 | |
| 向后兼容 | 按需 | |

## 用例列表

### AC-1 `<标题>`

- 输入：
- 操作：
- 预期行为：
- 验证方法（命令）：
- 证据路径：
- 结果：PASS / FAIL

> 不适用项必须记录理由；禁止用"总覆盖率"替代逐条证据。
